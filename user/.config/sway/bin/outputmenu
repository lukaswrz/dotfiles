#!/usr/bin/env oil

if (len(ARGV) !== 0) {
  exit 1
}

const output = $(outputassign list | bemenu --ignorecase --list 10 --prompt outputs)

const pos = $(seq 1 12 | bemenu --list 12 --prompt pos)

outputassign set -- $output $pos
